on:
  workflow_dispatch:

jobs:
  run-solution-workflow:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Packages
        run: pip install -r requirements.txt

      - name: Execute Workflow
        run: python workflows/solution_designer_workflow.py